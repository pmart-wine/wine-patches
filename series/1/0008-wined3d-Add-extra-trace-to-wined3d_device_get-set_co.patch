From a4349405d8c003acb81b3ac06cc5a9c85799bdc1 Mon Sep 17 00:00:00 2001
From: Pablo Martin <pmartXXXXXXXXX>
Date: Mon, 28 May 2018 04:07:15 +0200
Subject: [PATCH 8/8] wined3d: Add extra trace to
 wined3d_device_get/set_constant_buffer.

Signed-off-by: Pablo Martin <pmartXXXXXXXXX>
---
 dlls/wined3d/device.c | 4 ++++
 1 file changed, 4 insertions(+)

diff --git a/dlls/wined3d/device.c b/dlls/wined3d/device.c
index dbcd039d90..899ec3343a 100644
--- a/dlls/wined3d/device.c
+++ b/dlls/wined3d/device.c
@@ -2232,6 +2232,8 @@ void CDECL wined3d_device_set_constant_buffer(struct wined3d_device *device,
 {
     struct wined3d_buffer *prev;
 
+    TRACE("device %p, type %#x, idx %u, buffer %p.\n", device, type, idx, buffer);
+
     if (idx >= MAX_CONSTANT_BUFFERS)
     {
         WARN("Invalid constant buffer index %u.\n", idx);
@@ -2254,6 +2256,8 @@ void CDECL wined3d_device_set_constant_buffer(struct wined3d_device *device,
 struct wined3d_buffer * CDECL wined3d_device_get_constant_buffer(const struct wined3d_device *device,
         enum wined3d_shader_type shader_type, unsigned int idx)
 {
+    TRACE("device %p, shader_type %#x, idx %u.\n", device, shader_type, idx);
+
     if (idx >= MAX_CONSTANT_BUFFERS)
     {
         WARN("Invalid constant buffer index %u.\n", idx);
-- 
2.14.1

